services:
  - type: web
    name: precious-peach-bot
    env: python
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: python web_server.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_GROUP_ID
        sync: false
    healthCheckPath: /