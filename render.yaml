services:
  - type: web
    name: precious-peaches-bot
    env: python
    pythonVersion: "3.11.9"
    buildCommand: pip install -r requirements.txt
    startCommand: python web_server.py
    envVars:
      - key: PORT
        value: 10000
